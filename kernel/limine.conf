# Nucleus boot menu entry (Limine v12.9.x config format).
# Installed at /EFI/BOOT/limine.conf on the ESP by scripts/build-iso.sh.

timeout: 3
serial: yes

/NOVA (Nucleus)
    protocol: limine
    path: boot(1):/NUCLEUS
    resolution: 1024x768x32
